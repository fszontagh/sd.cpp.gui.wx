#ifndef __INPAINT_EDITOR__
#define __INPAINT_EDITOR__
class InpaintEditor : public wxPanel
{
public:
    InpaintEditor(wxWindow* parent, wxWindowID id, const wxPoint& pos, const wxSize& size);
    ~InpaintEditor();
};

#endif